services:
  aure-frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: aure-frontend
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - NEXT_PUBLIC_API_URL=https://aureapi.gabrielsanztech.com.br/api
    networks:
      - aure-network
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:3000/api/health', (r) => {process.exit(r.statusCode === 200 ? 0 : 1)})"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # Desenvolvimento (comentado por padrão)
  # aure-frontend-dev:
  #   build:
  #     context: .
  #     dockerfile: Dockerfile.dev
  #   container_name: aure-frontend-dev
  #   restart: unless-stopped
  #   ports:
  #     - "3000:3000"
  #   volumes:
  #     - .:/app
  #     - /app/node_modules
  #     - /app/.next
  #   environment:
  #     - NODE_ENV=development
  #     - NEXT_PUBLIC_API_URL=http://localhost:5203/api
  #   networks:
  #     - aure-network
  #   command: npm run dev

networks:
  aure-network:
    driver: bridge
